# Azure OpenAI Configuration
# Get these values from your Azure Portal (Azure OpenAI resource)

# Required: Your Azure OpenAI endpoint URL
# Example: https://your-resource-name.openai.azure.com
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com

# Required: Your deployment name for the chat model
# This is the name you gave when deploying the model (e.g., gpt-4o-mini, gpt-4, gpt-35-turbo)
AZURE_OPENAI_CHAT_DEPLOYMENT_NAME=gpt-4o-mini

# Optional: API version (defaults to 2024-10-21 if not specified)
AZURE_OPENAI_API_VERSION=2024-10-21

# Optional: API Key (only if not using Azure CLI authentication)
# If this is not set, the scripts will use Azure CLI authentication (az login)
# AZURE_OPENAI_API_KEY=your-api-key-here

# ============================================================================
# SETUP INSTRUCTIONS:
# ============================================================================
# 1. Copy this file and rename it to .env (remove .sample)
# 2. Fill in your actual Azure OpenAI values
# 3. Choose authentication method:
#
#    Option A - Azure CLI (Recommended):
#      - Run: az login
#      - Leave AZURE_OPENAI_API_KEY commented out
#
#    Option B - API Key:
#      - Uncomment AZURE_OPENAI_API_KEY
#      - Add your API key from Azure Portal
#
# 4. Ensure you have deployed a chat model in Azure OpenAI Studio
# 5. Update AZURE_OPENAI_CHAT_DEPLOYMENT_NAME with your deployment name
# ============================================================================

# ============================================================================
# ADDITIONAL NOTES:
# ============================================================================
# - For Magentic orchestration with web search, you may need gpt-4o-search-preview
# - Ensure your Azure OpenAI resource has sufficient quota
# - The scripts use AzureCliCredential by default for authentication
# ============================================================================
